version: '3.8'
services:
  node_test:
    build:
      context: ./
      dockerfile: Dockerfile.node
    volumes:
      - ./hathor/nanocontracts/blueprints:/app/hathor/hathor/nanocontracts/blueprints
      - ./hathor/conf/nano_testnet.yml:/app/hathor/hathor/conf/nano_testnet.yml
      - ./tests:/app/hathor/tests

   # command: ["poetry", "run", "pytest","-rP", "hathor/tests/nanocontracts/blueprints/test_rewards.py"] # with stdout
    command: ["poetry", "run", "pytest", "tests/nanocontracts/test_call_other_contract.py"] # without stdout
